{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the GameTracker Remote companion.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
