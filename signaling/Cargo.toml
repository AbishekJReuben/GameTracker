[package]
name = "gametracker-signal"
version = "0.1.0"
edition = "2021"
description = "Rendezvous/signaling server for GameTracker Remote WebRTC connections"

[[bin]]
name = "gametracker-signal"
path = "src/main.rs"

[dependencies]
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
futures-util = "0.3"
serde = { version = "1", features = ["derive"] }
# Serves the built Meta Quest client (signaling/static) so the headset can open
# it over the same Cloudflare HTTPS tunnel — WebXR requires a secure context.
tower-http = { version = "0.5", features = ["fs"] }

[profile.release]
opt-level = "s"
lto = true
strip = true
